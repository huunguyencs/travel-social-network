import { makeStyles } from "@material-ui/core";

const eventStyles = makeStyles((theme) => ({
    eventCardContainer: {
        width: 400,
        margin: 20,
    },
    media: {
        height: 300,
    }
}))


export default eventStyles;